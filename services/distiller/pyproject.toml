[project]
name = "digital-persona-distiller"
version = "0.1.0"
description = "Minimal persona distillation worker"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0.0,<9.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
